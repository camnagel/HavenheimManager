<UserControl x:Class="HavenheimManager.Controllers.TraitControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d">
    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Width="2360" Margin="10,10,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Width="1500" Height="20">
                    <TextBlock>Name</TextBlock>
                    <TextBlock Margin="180, 0, 0, 0">Filters</TextBlock>
                    <TextBlock Margin="188, 0, 0, 0">Selected Trait</TextBlock>
                    <TextBlock Margin="610, 0, 0, 0">Favorites</TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Width="1500" Height="900">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Right" Width="200" Height="900"
                                Margin="0,0,10,0">
                        <TextBox Text="{Binding TraitHandler.TraitSearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"
                                 Height="20">
                            <b:Interaction.Triggers>
                                <b:EventTrigger EventName="LostFocus">
                                    <b:InvokeCommandAction Command="{Binding TraitHandler.TraitSearchAddPlaceholderTextCommand}" />
                                </b:EventTrigger>
                                <b:EventTrigger EventName="GotFocus">
                                    <b:InvokeCommandAction Command="{Binding TraitHandler.TraitSearchRemovePlaceholderTextCommand}" />
                                </b:EventTrigger>
                            </b:Interaction.Triggers>
                        </TextBox>
                        <ListBox ItemsSource="{Binding TraitHandler.FilteredTraitList}" Background="DimGray" Width="200"
                                 Height="875"
                                 SelectedItem="{Binding TraitHandler.SelectedTrait, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Foreground="AliceBlue">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical">
                                        <TextBlock Text="{Binding Path=Name}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>

                    <StackPanel Orientation="Vertical" HorizontalAlignment="Right" Width="200" Height="900"
                                Margin="0,0,20,0">
                        <Expander Header="Core" FontSize="10" FontWeight="Bold">
                            <ListBox ItemsSource="{Binding TraitHandler.CoreTraitFilterList}" Background="LightGray">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical">
                                            <CheckBox FontWeight="Normal"
                                                      Command="{Binding RelativeSource=
                                        {RelativeSource AncestorType=ListBox},
                                        Path=DataContext.TraitHandler.CoreTraitCheckboxCommand}"
                                                      CommandParameter="{Binding}"
                                                      Content="{Binding}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Expander>

                        <Expander Header="Skills" FontSize="10" FontWeight="Bold">
                            <ListBox ItemsSource="{Binding TraitHandler.SkillTraitFilterList}" Height="500"
                                     ScrollViewer.VerticalScrollBarVisibility="Disabled" Background="LightGray">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical">
                                            <CheckBox FontWeight="Normal"
                                                      Command="{Binding RelativeSource=
                                        {RelativeSource AncestorType=ListBox},
                                        Path=DataContext.TraitHandler.SkillTraitCheckboxCommand}"
                                                      CommandParameter="{Binding}"
                                                      Content="{Binding}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Expander>

                        <Expander Header="Class" FontSize="10" FontWeight="Bold">
                            <ListBox ItemsSource="{Binding TraitHandler.ClassTraitFilterList}" Height="500"
                                     ScrollViewer.VerticalScrollBarVisibility="Disabled" Background="LightGray">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical">
                                            <CheckBox FontWeight="Normal"
                                                      Command="{Binding RelativeSource=
                                        {RelativeSource AncestorType=ListBox},
                                        Path=DataContext.TraitHandler.ClassTraitCheckboxCommand}"
                                                      CommandParameter="{Binding}"
                                                      Content="{Binding}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Expander>

                        <Expander Header="Combat" FontSize="10" FontWeight="Bold">
                            <ListBox ItemsSource="{Binding TraitHandler.CombatTraitFilterList}" Height="500"
                                     ScrollViewer.VerticalScrollBarVisibility="Disabled" Background="LightGray">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical">
                                            <CheckBox FontWeight="Normal"
                                                      Command="{Binding RelativeSource=
                                        {RelativeSource AncestorType=ListBox},
                                        Path=DataContext.TraitHandler.CombatTraitCheckboxCommand}"
                                                      CommandParameter="{Binding}"
                                                      Content="{Binding}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Expander>

                        <Expander Header="Role" FontSize="10" FontWeight="Bold">
                            <ListBox ItemsSource="{Binding TraitHandler.RoleTraitFilterList}" Height="500"
                                     ScrollViewer.VerticalScrollBarVisibility="Disabled" Background="LightGray">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical">
                                            <CheckBox FontWeight="Normal"
                                                      Command="{Binding RelativeSource=
                                        {RelativeSource AncestorType=ListBox},
                                        Path=DataContext.TraitHandler.RoleTraitCheckboxCommand}"
                                                      CommandParameter="{Binding}"
                                                      Content="{Binding}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Expander>

                        <Expander Header="Spells" FontSize="10" FontWeight="Bold">
                            <ListBox ItemsSource="{Binding TraitHandler.MagicTraitFilterList}" Background="LightGray">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical">
                                            <CheckBox FontWeight="Normal"
                                                      Command="{Binding RelativeSource=
                                        {RelativeSource AncestorType=ListBox},
                                        Path=DataContext.TraitHandler.MagicTraitCheckboxCommand}"
                                                      CommandParameter="{Binding}"
                                                      Content="{Binding}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Expander>

                        <Expander Header="Bonus" FontSize="10" FontWeight="Bold">
                            <ListBox ItemsSource="{Binding TraitHandler.BonusTraitFilterList}" Background="LightGray">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical">
                                            <CheckBox FontWeight="Normal"
                                                      Command="{Binding RelativeSource=
                                        {RelativeSource AncestorType=ListBox},
                                        Path=DataContext.TraitHandler.BonusTraitCheckboxCommand}"
                                                      CommandParameter="{Binding}"
                                                      Content="{Binding}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Expander>

                        <Expander Header="Condition" FontSize="10" FontWeight="Bold">
                            <ListBox ItemsSource="{Binding TraitHandler.ConditionTraitFilterList}" Height="500"
                                     ScrollViewer.VerticalScrollBarVisibility="Disabled" Background="LightGray">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical">
                                            <CheckBox FontWeight="Normal"
                                                      Command="{Binding RelativeSource=
                                        {RelativeSource AncestorType=ListBox},
                                        Path=DataContext.TraitHandler.ConditionTraitCheckboxCommand}"
                                                      CommandParameter="{Binding}"
                                                      Content="{Binding}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Expander>

                        <Expander Header="Source" FontSize="10" FontWeight="Bold">
                            <ListBox ItemsSource="{Binding TraitHandler.SourceTraitFilterList}" Background="LightGray">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical">
                                            <CheckBox FontWeight="Normal"
                                                      Command="{Binding RelativeSource=
                                        {RelativeSource AncestorType=ListBox},
                                        Path=DataContext.TraitHandler.SourceTraitCheckboxCommand}"
                                                      CommandParameter="{Binding}"
                                                      Content="{Binding}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Expander>

                        <Expander Header="Custom" FontSize="10" FontWeight="Bold">
                            <ListBox ItemsSource="{Binding TraitHandler.CustomTraitFilterList}" Height="500"
                                     ScrollViewer.VerticalScrollBarVisibility="Disabled" Background="LightGray">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical">
                                            <CheckBox FontWeight="Normal"
                                                      Command="{Binding RelativeSource=
                                        {RelativeSource AncestorType=ListBox},
                                        Path=DataContext.TraitHandler.CustomTraitCheckboxCommand}"
                                                      CommandParameter="{Binding}"
                                                      Content="{Binding}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Expander>
                    </StackPanel>

                    <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top" Width="500"
                                Height="900">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top"
                                    Width="500" Height="20">
                            <TextBlock Margin="0, 0, 3, 0">Name:</TextBlock>
                            <TextBox Text="{Binding TraitHandler.SelectedTrait.Name}" Width="256" Height="20" Background="DimGray"
                                     Foreground="AliceBlue" IsReadOnly="True" />

                            <TextBlock Margin="15, 0, 3, 0">Source:</TextBlock>
                            <TextBox Text="{Binding TraitHandler.SelectedTrait.Source}" Width="150" Height="20" Background="DimGray"
                                     Foreground="AliceBlue" IsReadOnly="True" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0, 5, 0, 0">
                            <TextBlock Margin="0, 0, 3, 0">Url:</TextBlock>
                            <TextBox Text="{Binding TraitHandler.SelectedTrait.Url}" Width="480" Height="20" Background="DimGray"
                                     Foreground="AliceBlue" IsReadOnly="True" />
                        </StackPanel>

                        <TextBlock Margin="0, 5, 0, 0">Benefits:</TextBlock>
                        <TextBox Text="{Binding TraitHandler.SelectedTrait.Description}" Width="500" Height="700"
                                 Background="DimGray" Foreground="AliceBlue" IsReadOnly="True" />

                        <TextBlock Margin="0, 5, 0, 0">Notes:</TextBlock>
                        <TextBox Text="{Binding TraitHandler.SelectedTrait.Notes}" Width="500" Height="115" Background="DimGray"
                                 Foreground="AliceBlue" IsReadOnly="False" />
                    </StackPanel>

                    <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top" Width="150"
                                Height="900" Margin="10, 0, 0, 0">
                        <TextBlock Margin="0, 0, 0, 0">Prereqs:</TextBlock>
                        <ListBox ItemsSource="{Binding TraitHandler.SelectedTrait.Prereqs}" Background="DimGray"
                                 Foreground="AliceBlue" Width="150" Height="117" />

                        <TextBlock Margin="0, 5, 0, 0">Custom Tags:</TextBlock>
                        <ListBox ItemsSource="{Binding TraitHandler.SelectedTrait.CustomTags}" Background="DimGray"
                                 Foreground="AliceBlue" Width="150" Height="75" />

                        <TextBlock Margin="0, 5, 0, 0">Core Tags:</TextBlock>
                        <ListBox ItemsSource="{Binding TraitHandler.SelectedTrait.CoreTags}" Background="DimGray"
                                 Foreground="AliceBlue" Width="150" Height="75" />

                        <TextBlock Margin="0, 5, 0, 0">Skill Tags:</TextBlock>
                        <ListBox ItemsSource="{Binding TraitHandler.SelectedTrait.SkillTags}" Background="DimGray"
                                 Foreground="AliceBlue" Width="150" Height="75" />

                        <TextBlock Margin="0, 5, 0, 0">Class Tags:</TextBlock>
                        <ListBox ItemsSource="{Binding TraitHandler.SelectedTrait.ClassTags}" Background="DimGray"
                                 Foreground="AliceBlue" Width="150" Height="75" />

                        <TextBlock Margin="0, 5, 0, 0">Combat Tags:</TextBlock>
                        <ListBox ItemsSource="{Binding TraitHandler.SelectedTrait.CombatTags}" Background="DimGray"
                                 Foreground="AliceBlue" Width="150" Height="75" />

                        <TextBlock Margin="0, 5, 0, 0">Role Tags:</TextBlock>
                        <ListBox ItemsSource="{Binding TraitHandler.SelectedTrait.RoleTags}" Background="DimGray"
                                 Foreground="AliceBlue" Width="150" Height="75" />

                        <TextBlock Margin="0, 5, 0, 0">Spell Tags:</TextBlock>
                        <ListBox ItemsSource="{Binding TraitHandler.SelectedTrait.MagicTags}" Background="DimGray"
                                 Foreground="AliceBlue" Width="150" Height="75" />

                        <TextBlock Margin="0, 5, 0, 0">Bonus Tags:</TextBlock>
                        <ListBox ItemsSource="{Binding TraitHandler.SelectedTrait.BonusTags}" Background="DimGray"
                                 Foreground="AliceBlue" Width="150" Height="75" />
                    </StackPanel>

                    <StackPanel Orientation="Vertical" Margin="20, 0, 0, 0" Width="150">
                        <ListBox ItemsSource="{Binding TraitHandler.FavoriteTraitList}" Background="DimGray" Foreground="AliceBlue"
                                 Width="150" Height="540"
                                 SelectedItem="{Binding TraitHandler.SelectedTrait, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical">
                                        <TextBlock Text="{Binding Path=Name}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <TextBlock Margin="0, 5, 0, 0">Hidden</TextBlock>
                        <Expander>
                            <ListBox ItemsSource="{Binding TraitHandler.HiddenTraitList}" Background="DimGray"
                                     Foreground="AliceBlue" Width="150" Height="317"
                                     SelectedItem="{Binding TraitHandler.SelectedTrait, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical">
                                            <TextBlock Text="{Binding Path=Name}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Expander>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="1500"
                            Height="50">
                    <Button Content="Favorite"
                            Command="{Binding TraitHandler.AddFavoriteTraitCommand}"
                            Width="60" Height="30" Margin="25,0,30,0" />

                    <Button Content="Hide"
                            Command="{Binding TraitHandler.AddHiddenTraitCommand}"
                            Width="60" Height="30" Margin="0,0,25,0" />

                    <Button Content="Show/Hide Filters"
                            Command="{Binding TraitHandler.ShowHideFilterCommand}"
                            Width="120" Height="30" Margin="50,0,40,0" />

                    <Button Content="Edit"
                            Command="{Binding TraitHandler.EditTraitCommand}"
                            Width="60" Height="30" Margin="120,0,0,0" />

                    <Button Content="New"
                            Command="{Binding TraitHandler.NewTraitCommand}"
                            Width="60" Height="30" Margin="60,0,60,0" />

                    <Button Content="Remove"
                            Command="{Binding TraitHandler.RemoveTraitCommand}"
                            Width="60" Height="30" Margin="0,0,100,0" />

                    <Button Content="Unfavorite"
                            Command="{Binding TraitHandler.RemoveFavoriteTraitCommand}"
                            Width="60" Height="30" Margin="190,0,10,0" />

                    <Button Content="Show"
                            Command="{Binding TraitHandler.RemoveHiddenTraitCommand}"
                            Width="60" Height="30" Margin="0,0,0,0" />
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>