using System.Collections.ObjectModel;

namespace AssetManager.Containers
{
    public class SerialBin
    {
        /*public ObservableCollection<Clip> ClipList { get; set; }
        public ObservableCollection<Clip> ReadyList { get; set; } 
        public ObservableCollection<Clip> ExportList { get; set; }

        public ObservableCollection<Name> HeadList { get; set; }

        public SerialBin(ObservableCollection<Clip> clipList, ObservableCollection<Clip> readyList, 
            ObservableCollection<Clip> exportList, ObservableCollection<Name> headList)
        {
            ClipList = clipList;
            HeadList = headList;
            ReadyList = readyList != null ? readyList : new();
            ExportList = exportList != null ? exportList : new();
        }*/
    }
}
